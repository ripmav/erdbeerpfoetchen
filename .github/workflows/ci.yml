name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - main

jobs:
  govulncheck:
    name: Govulncheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.3.0

      - name: Run govulncheck
        id: govulncheck
        continue-on-error: true
        run: |
          set -o pipefail
          govulncheck ./... 2>&1 | tee govulncheck_output.txt

      - name: Annotate action with findings
        if: steps.govulncheck.outcome == 'failure'
        run: |
          echo "::error title=govulncheck::Vulnerability found — see workflow summary for details"
          {
            echo "## govulncheck findings"
            echo '```'
            cat govulncheck_output.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Post PR comment on findings
        if: steps.govulncheck.outcome == 'failure' && github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo '## :warning: govulncheck findings'
            echo ''
            echo '```'
            cat govulncheck_output.txt
            echo '```'
            echo ''
            echo "See the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details."
          } > pr_comment.txt
          gh pr comment "${{ github.event.pull_request.number }}" --body-file pr_comment.txt

      - name: Fail on findings
        if: steps.govulncheck.outcome == 'failure'
        run: exit 1

  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    # Skip PRs from docs/ or ci/ branches, and commits with those prefixes on main
    if: |
      !startsWith(github.head_ref, 'docs/') &&
      !startsWith(github.head_ref, 'ci/') &&
      !startsWith(github.event.head_commit.message, 'docs:') &&
      !startsWith(github.event.head_commit.message, 'ci:')
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Install golangci-lint
        run: curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.12.2

      - name: Run golangci-lint
        id: lint
        continue-on-error: true
        run: |
          set -o pipefail
          golangci-lint run ./... 2>&1 | tee lint_output.txt

      - name: Annotate action with findings
        if: steps.lint.outcome == 'failure'
        run: |
          echo "::error title=golangci-lint::Lint findings — see workflow summary for details"
          {
            echo "## golangci-lint findings"
            echo '```'
            cat lint_output.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Post PR comment on findings
        if: steps.lint.outcome == 'failure' && github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo '## :warning: golangci-lint findings'
            echo ''
            echo '```'
            cat lint_output.txt
            echo '```'
            echo ''
            echo "See the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details."
          } > lint_comment.txt
          gh pr comment "${{ github.event.pull_request.number }}" --body-file lint_comment.txt

      - name: Fail on findings
        if: steps.lint.outcome == 'failure'
        run: exit 1

  test:
    name: Test
    runs-on: ubuntu-latest
    if: |
      !startsWith(github.head_ref, 'docs/') &&
      !startsWith(github.head_ref, 'ci/') &&
      !startsWith(github.event.head_commit.message, 'docs:') &&
      !startsWith(github.event.head_commit.message, 'ci:')
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: go test -race ./...

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [govulncheck]
    if: |
      !startsWith(github.head_ref, 'docs/') &&
      !startsWith(github.head_ref, 'ci/') &&
      !startsWith(github.event.head_commit.message, 'docs:') &&
      !startsWith(github.event.head_commit.message, 'ci:')
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        run: go build ./cmd/api
